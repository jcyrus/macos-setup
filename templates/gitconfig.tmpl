# Git configuration generated by macos-setup
[core]
    pager = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true
    dark = true
    line-numbers = true
    side-by-side = __DELTA_SIDE_BY_SIDE__

[merge]
    conflictstyle = zdiff3
